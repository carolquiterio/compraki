import React from 'react';

import {Container, StyledText} from './styles';

const Home = () => {
  return (
    <Container>
      <StyledText></StyledText>
    </Container>
  );
};

export default Home;
